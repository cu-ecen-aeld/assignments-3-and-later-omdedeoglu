# Compiler and flags
CC = gcc
CFLAGS = -Wall -Wextra -O2 -std=c99

# Target executable
TARGET = writer

# Source file
SRC = writer.c

# Default rule
all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) $(SRC) -o $(TARGET)

# Clean rule
clean:
	rm -f $(TARGET)